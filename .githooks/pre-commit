#!/usr/bin/env bash
set -euo pipefail

echo "Checking formatting, tests, lints, and Nix evaluation..."
cargo fmt --all -- --check
cargo test --workspace
cargo clippy --workspace --all-targets --all-features -- -D warnings
nix flake check --no-build
